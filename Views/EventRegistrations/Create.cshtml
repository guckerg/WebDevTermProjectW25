@model EventManager.Models.ViewModels.EventRegistrationViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>EventRegistration</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
              <label asp-for="EventRegistration.UserID" class="control-label"></label>
              <select asp-for="EventRegistration.UserID" class="form-control" asp-items="Model.Users"></select>
              <span asp-validation-for="EventRegistration.UserID" class="text-danger"></span>
            </div>
            <div class="form-group">
               <label asp-for="EventRegistration.EventID" class="control-label"></label>
               <select asp-for="EventRegistration.EventID" class="form-control" asp-items="Model.Events"></select>
               <span asp-validation-for="EventRegistration.EventID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>

    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
